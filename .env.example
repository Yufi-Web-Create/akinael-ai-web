# Default is a non-indexable release candidate. Set true only after publication approval.
SITE_INDEXABLE=false
# Authentication and customer data remain in the Core-owned Portal.
# No API keys or secrets are required for the marketing site.
